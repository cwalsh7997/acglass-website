# Dealer application pages

Public static pages for the dealer-application flow (`admin.html`, `login.html`,
`thanks.html`, and the form at `/become-a-dealer.html`).

Worker deploy, D1, and admin-token notes live in `.github/docs/dealer-README.md`
(repo-only; GitHub Pages does not serve `.github/`).
