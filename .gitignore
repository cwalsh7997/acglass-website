.vercel

__pycache__/
*.pyc

# internal ops/build files — never deploy
_internal/
__pycache__/

# local agent + design tooling config, root-level by necessity
# (these cannot live under _internal/ because the tools resolve them from the repo root)
.claude/
.impeccable/

# agent kit. CLAUDE.md must sit at the repo root for Claude Code to auto-load it,
# but the root is the deploy root. Untracked keeps it local-only and undeployable.
/CLAUDE.md
