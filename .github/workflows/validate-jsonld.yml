name: Validate JSON-LD

# Runs against the checked-out tree, not the live site, so a broken entity
# graph is caught on the PR instead of after it deploys.
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Representative page classes
        run: python .github/scripts/validate-jsonld.py --sample -v

      - name: Full site
        run: python .github/scripts/validate-jsonld.py
